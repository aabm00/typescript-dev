// Cuando a una variable se declara y aplica un valor
// al mismo tiempo, implicitamente detecta el tipo
const today = new Date();
today.getMonth();

const person = {
  age: 20,
};

class Color {}
const red = new Color();
